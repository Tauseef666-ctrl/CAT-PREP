# Copy to `.env.local` only if you add server integrations.
# None are required to run the app — everything works offline with localStorage.

# Optional future integrations (leave blank when unused):
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=